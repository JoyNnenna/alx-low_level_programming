#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Starting point
 * Description: Prints lowercase alphabets in c
 * Return: return (0) (Successful)
*/
int main(void)
{

int ch;

for (ch = 'a' ; ch <= "z" ; ch++)
{
	putchar(ch);
}
return (0);

}
